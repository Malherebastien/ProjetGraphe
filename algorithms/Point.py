class Point(object):
    """
    Objet Point

    Paramètres :
    x : coordonnée x du point
    y : coordonnée y du point
    """
    def __init__(self, point_id, x, y):
        self.point_id = point_id
        self.x = x
        self.y = y
